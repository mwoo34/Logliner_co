using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameFailPlay : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameObj.checkGameSuccess = 1;   
    }
}
